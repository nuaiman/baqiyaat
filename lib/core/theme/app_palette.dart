import 'package:flutter/material.dart';

class AppPalette {
  static const darkBg = Color(0xFF000000);
  static const lightBg = Color(0xFFffffff);
  // ---------------------------------------------------------------------------
  static const darkCard = Color(0xFF0a0a0a);
  static const lightCard = Color(0xFFf9f9fa);
  // ---------------------------------------------------------------------------
  static const active = Color(0xFF3872f5);
  static const inactive = Color(0xFFf6f6f6);
  // ---------------------------------------------------------------------------
  static const darkTitle = Color(0xFF6c6c6c);
  static const lightTitle = Color(0xFF959596);
  // ---------------------------------------------------------------------------
  static const darkBorder = Color(0xFF2f2f2f);
  static const lighBorder = Color(0xFFe0e0e1);
  // ---------------------------------------------------------------------------
  static const black = Color(0xFF000000);
  static const red = Color(0xFFff3440);
  static const white = Color(0xFFffffff);
  // ---------------------------------------------------------------------------
  static const transparent = Colors.transparent;
}

// class AppPalette {
//   static const darkBg = Color(0xFF0a0a0a);
//   static const lightBg = Color(0xFFf3f3f3);
//   // ---------------------------------------------------------------------------
//   static const darkCard = Color(0xFF181818);
//   static const lightCard = Color(0xFFffffff);
//   // ---------------------------------------------------------------------------
//   static const active = Color(0xFF3872f5);
//   static const inactive = Color(0xFFf6f6f6);
//   // ---------------------------------------------------------------------------
//   static const black = Color(0xFF000000);
//   static const grey = Color(0xFFb0b0b0);
//   static const red = Color(0xFFff3440);
//   static const white = Color(0xFFffffff);
//   // ---------------------------------------------------------------------------
//   static const transparent = Colors.transparent;
// }
